{0}

// underscores are automatically added to the beginning of each value's name to avoid 
// name conflicts with reserved keywords

using UnityEngine;
namespace InflamedGums.Util.ScriptableVariables
{{
    [CreateAssetMenu(menuName = "Scriptable Objects/Enum Types/{1}")]
    public class {1}Variable : EnumType<{1}Variable.{1}>
    {{
        public enum {1} 
        {{ 
            {2} 
        }}

        public void Reset() => m_Value = {1}._{3};
    }}
}}
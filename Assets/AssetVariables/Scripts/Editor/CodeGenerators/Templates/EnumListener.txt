using UnityEngine;
using UnityEngine.Events;

namespace LovelyBytes.AssetVariables
{
    using eType = %TYPENAME%Variable.%TYPENAME%;
    
    [AddComponentMenu(Constants.DefaultAssetPath + "%TYPENAME% Listener")]
    public class %TYPENAME%Listener : VariableListener<%TYPENAME%Variable, eType>
    {
    }
}

